<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/chdtu_value2</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CHDTU"></style>

    <style name="CHDTU.InputText">
       <item name="android:background">@drawable/roundborders_rect_shape</item>
        <item name="android:textColor">@color/gray2</item>
        <item name="android:textColorHint">@color/gray2</item>
        <item name="android:colorAccent">@color/gray2</item>
    </style>

    <style name="CHDTU.AcitivityBackground" parent="Theme.AppCompat.Light">
        <!-- This is the styling for action bar -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid">
        <item name="windowNoTitle">true</item>
        <item name="background">@color/chdtu_value1</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MyActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
    <item name="android:textColor">@color/white</item>
    </style>

    <style name="ProgressBar" parent="@android:style/Widget.ProgressBar">
        <item name="colorAccent">@color/chdtu_value2</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:visibility">gone</item>
        <item name="android:background">@drawable/round_rect_shape_white</item>
        <item name="android:indeterminateDrawable">@drawable/chdtu_progressbar</item>
    </style>

    <style name="SelectedElement" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/disable_text_color</item>
    </style>

</resources>
