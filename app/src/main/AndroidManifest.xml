<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.edu.deanoffice.mobile.studentchdtu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CHDTU.AcitivityBackground"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".user.profile.activity.MainOptionsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".user.profile.activity.MainMenuActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".applications.activity.ChooseApplicationActivity" />
        <activity
            android:name=".applications.activity.ExamApplicationActivity"
            android:label="@string/title_activity_exam_application" />
        <activity android:name=".applications.activity.DeducApplicationActivity" />
        <activity android:name=".applications.activity.RetakeApplicationActivity" />
        <activity android:name=".applications.activity.RenewApplicationActivity" />
        <activity
            android:name=".course.selective.view.activity.SelectiveCoursesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".user.login.activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>